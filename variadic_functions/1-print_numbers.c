#include "variadic_functions.h"
/**

*/
void print_numbers(const char *separator, const unsigned int n, ...)
{
    int i;
    for (i = 0; i < n; i++)
    {
        printf ("%d separator \n" i);
    }
    if (separator != NULL)
    i++;

    else
    print (" ")

    putchar ("\n")
}
